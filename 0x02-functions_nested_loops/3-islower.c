#include "main.h"
/**
 * _islower - prints if character is lower or uppercase
 *Return: for c 1 and 0
 *@c: character to be checked
 */
int _islower(int c)
{
if (c == 97 && 122)
{
	return (1);
}
else
{
	return (0);
}
}
