#include<stdio.h>
/**
 * main - prints all single digit numbers of base 10 from 0
 * Return: 0 when all is successful
*/
int main (void)
{
int x;
for (x= 48 ; x<=58 ; x++)
{
putchar(x);
}
putchar(10);

return (0);
}
